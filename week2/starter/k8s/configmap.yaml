# ConfigMap: store non-secret configuration (bucket names, flags, etc)
# Deployment will reference this to inject env vars into pods

apiVersion: v1
kind: ConfigMap
metadata:
  name: demand-api-config
data:
  GCS_BUCKET: "ops-ai-ysais-martinez-data" 
